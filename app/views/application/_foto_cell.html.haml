.foto_cell
  %label
    .img{:data => { :type => :foto }}
      = image_tag foto_file_path(foto)
    .icons
      = form.input_field :_id, :as => :boolean, :checked => false, :class => 'icon-ok-sign add'
  .icons
    = link_to "#", :class => 'tagged' do
      %i.icon-bookmark
    .zoomin
      %i.icon-zoom-in